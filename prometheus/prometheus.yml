global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:

  - job_name: "pq_reverse_proxy"

    scrape_interval: 5s

    static_configs:
      - targets:
          - "host.docker.internal:9090"