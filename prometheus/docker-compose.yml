services:

  prometheus:
    image: prom/prometheus:latest

    container_name: pq-prometheus

    ports:
      - "9091:9090"

    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro

      - prometheus_data:/prometheus

    command:
      - "--config.file=/etc/prometheus/prometheus.yml"

volumes:
  prometheus_data: